[project]
name = "mysql"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pymysql>=1.1.2",
]

[tool.uv.workspace]
members = [
    "vevn",
]
